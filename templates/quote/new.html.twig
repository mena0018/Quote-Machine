{% extends 'base.html.twig' %}

{% block title %}Ajouter une citation{% endblock %}

{% block body %}

<div class="container-fluid">
    <h1 class="py-4">Créer une citation</h1>

    {{ form_start(quoteForm) }}
    {{ form_widget(quoteForm) }}
    {{ form_end(quoteForm) }}
</div>
{% endblock %}