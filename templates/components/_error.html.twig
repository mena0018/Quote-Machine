{% extends 'base.html.twig' %}

{% block body %}
	<div class="d-flex align-items-center justify-content-center w-100 h-100">
		<div class="p-5 rounded-3">
			<div class="card-body d-flex flex-column align-items-center">
				<h5 class="card-title font-bold text-primary fs-1">HTTP Error
					{{ exception.statusCode }}</h5>
				<p class="card-text fs-4 text-secondary">{{ exception.statusText }}</p>
				<a href="/" class="btn btn-primary">Retour sur la page d'acceuil</a>
			</div>
		</div>
	</div>
{% endblock %}
